
// ActivityFeed removed — placeholder kept to avoid build errors if referenced elsewhere.
import React from 'react';

const ActivityFeed: React.FC<any> = () => {
  return null;
};

export default ActivityFeed;
